# Can solve the second layer (inserts memorised, not yet automatic)

Matt can solve the first two layers (F2L by beginner method). The two mirror-image edge inserts from lesson 0003 are committed to memory — he can recall and execute them — but they are not yet in muscle memory the way `R U R' U'` is. Self-described: "in my memory, but not in my hands yet".

## Evidence

Self-reported confidence after practising with lesson 0003, including handling wrongly-slotted edges.

## Implications

- Two-thirds of the cube is now solved unaided. Only the last layer remains.
- Fluency with the inserts will come for free, since every future full solve practises them — no dedicated drilling needed.
- Ready for stage 4: the **yellow cross** (`F R U R' U' F'`), the first pure last-layer algorithm. It reuses the `R U R' U'` trigger sandwiched in `F ... F'`, so memorisation load is low.
- From here on, every stage is algorithm-driven rather than intuitive. Lessons should lean harder on "what is this algorithm doing" to satisfy the mission's understanding criterion.
