# Can align the yellow cross edges

Matt can take a finished yellow cross and align its edges to the side centers: spin-and-count (always 2 or 4 matches), adjacent case (pair to back-right, swap once), opposite case (break it first, then swap). Concept fully understood; the `R U R' U R U2 R' U` swap algorithm is memorised but will need more reps to become automatic.

## Evidence

First real solve hit the lucky all-four-match case; he then used the algorithm-as-its-own-scrambler loop to drill the swap repeatedly, plus fresh scrambles for the opposite case.

## Implications

- Same pattern as record 0003: concept lands immediately, muscle memory trails behind. Full solves will bed it in — no dedicated drilling needed.
- The "algorithm is its own scrambler" practice loop worked well — reusable trick for the remaining two stages (both also only touch unsolved pieces).
- Cube state from here: only the four yellow corners unsolved. Ready for stage 6: positioning the yellow corners (`U R U' L' U R' U' L` three-corner cycle).
